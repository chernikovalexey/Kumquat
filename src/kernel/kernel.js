/*
 * 
**/

(function(win, doc, undefined) {
  
  // Add to the global scope
  win.ke = {};
  
  // Import function, analogue of `import` operator in Java
  ke.import = (function() {
    
    return function(src) {
      
    };
    
  })();
  
})(this, document);